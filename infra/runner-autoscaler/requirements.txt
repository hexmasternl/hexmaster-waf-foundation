azure-identity==1.19.0
# Pinned per Azure Functions Python advisory (cryptography maintainer breaking
# changes after 2025-11-27). Flex Consumption uses a newer Linux base image
# (glibc 2.35+), so cryptography 43.x manylinux_2_28 wheels load correctly.
cryptography==43.0.3
